{
  "name": "tridentfunc",
  "version": "1.0.1",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "installPrereqs": "npm install",
    "tridentFuncStart": "func start",
    "test": "echo \"No tests yet...\"",
    "updateToken": "vsts-npm-auth -config .npmrc -F"
  },
  "dependencies": {},
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "azure-functions-core-tools": "^4.x",
    "@types/node": "16.x",
    "typescript": "^4.0.0",
    "vsts-npm-auth": "0.42.1"
  },
  "bundledDependencies": [
    "@azure/functions",
    "azure-functions-core-tools"
  ]
}